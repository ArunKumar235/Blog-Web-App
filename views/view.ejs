<%- include("partials/header.ejs")  %>

<div class="container">
    <% if (blogs.length===0) { %>
        <div class="text-center p-4 m-4">
            <h1>Oops! it's empty</h1>
            <h2>Nothing to show...</h2>
        </div>
    <% } else { %>
            <% blogs.forEach( (blog) => { %>
            <article class="blog-post container">
                <h1 name="title"><%= blog.title %></h1>
                <h2 name="author">By <%= blog.author %></h2>                
                <p class="fs-4" name="content"><%= blog.content %></p>

                <form action="/update" method="POST">
                    <input type="hidden" name="title" value="<%= blog.title %>">
                    <input type="hidden" name="author" value="<%= blog.author %>">
                    <input type="hidden" name="content" value="<%= blog.content %>">
                    <button type="submit" class="btn btn-outline-primary">Update</button>
                </form>

                
            </article>
            <hr>
            <% }); %>
    <% } %>
</div>


<%- include("partials/footer.ejs") %>